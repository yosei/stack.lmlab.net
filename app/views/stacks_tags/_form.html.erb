<%= form_for(@stacks_tag) do |f| %>
  <% if @stacks_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stacks_tag.errors.count, "error") %> prohibited this stacks_tag from being saved:</h2>

      <ul>
      <% @stacks_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :stack_id %><br>
    <%= f.number_field :stack_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
